# Сервис Event Consumer

Сервис служит для работы с внутренними событиями, приходящие в RabbitMQ.
Проект написан на Python 3.14 + UV

## Используемые инструменты

- FastStream - Основной фреймворк RMQ
- TaskIQ - Фреймворк фоновых задач
- Pydantic - Валидация/трансформация данных


## Структура проекта

```sh
event_consumer/
├── 📁 faststream_core/
│   └── broker.py                # Конфигурация соединения с RMQ + базовый консюмер
├── 📁 taskiq_core/              # Фоновые операции
│   ├── broker.py                # Кофигурации подключения воркера TaskIQ
│   └── tasks.py                 # Текущие фоновые задачи
├── configs.py                   # Модуль конфигурации и подтягивания .env
├── FastStream_Dockerfile        # Правила развёртывания докер образа FastStream
├── TaskIQ_Dockerfile            # Правила развёртывания докер образа TaskIQ
├── main.py                      # Модуль запуска
├── .env.local                   # Пример переменных окружения
└── pyproject.toml               # Конфигурации проекта
```

## Переменный окружения

```sh
# Конфиги подключения в RabbitMQ
RMQ_USER=user
RMQ_PASSWORD=bitnami
RMQ_HOST=localhost
RMQ_PORT=5672
RMQ_VHOST=%2f
RMQ_PARAMS=connection_attempts=3&heartbeat=60
RMQ_EXCHANGE=test
RMQ_QUEUE=test

LOGLEVEL=INFO
```
